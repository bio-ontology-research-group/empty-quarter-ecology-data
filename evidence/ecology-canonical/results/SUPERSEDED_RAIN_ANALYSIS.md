# Superseded rainfall artifacts

Status: **SUPERSEDED**

The files `rain_window_models.tsv` and `rain_leave_one_campaign.tsv`, the rainfall panels formerly shown in `claim_rescue_summary.pdf`, and the two rainfall rows in `claim_ledger.tsv`/`.json` are historical diagnostics. They must not be used as manuscript evidence.

Replacement: `analysis/v3/rain_response_window/`

Reason: Historical compartment-wise screen; superseded because it does not estimate the paired surface-minus-shallow contrast with site- and campaign-specific position baselines or calibrate the complete window search while preserving the spatial rainfall field.
